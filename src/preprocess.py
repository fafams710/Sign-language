"""Landmark preprocessing for model training.

Normalizes landmark coordinates, pads or trims sequences to a fixed length,
and produces train / validation / test splits from the collected dataset.
"""

# TODO (Phase 2 - Dataset collection & preprocessing): implement normalization,
# fixed-length sequence preparation, and train/val/test splitting.

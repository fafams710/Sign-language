"""Hand and pose landmark extraction.

Wraps MediaPipe to detect hand (and optionally pose) landmarks from each frame
and return normalized coordinate features for downstream sequence processing.
"""

# TODO (Phase 1 - Webcam & landmark prototype): implement MediaPipe landmark extraction.

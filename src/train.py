"""Sign-classifier training (PyTorch).

Trains a landmark-sequence sign classifier using PyTorch. The model is a
sequence classifier (LSTM / GRU / 1D CNN / MLP) over fixed-length landmark
sequences, exported as ``sign_classifier.pt`` together with a label encoder.
"""

# TODO (Phase 3 - Baseline classifier): implement the PyTorch training loop.

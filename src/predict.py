"""Real-time inference and prediction smoothing.

Loads the trained classifier, runs inference over the live landmark sequence
buffer, and applies confidence / stability smoothing to produce stable
recognized sign tokens.
"""

# TODO (Phase 4 - Real-time prediction): implement live inference and smoothing.
